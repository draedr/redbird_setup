{
  "name": "redbird_setup",
  "version": "1.0.0",
  "description": "My setup for redbird proxy with custom resolvers for subfolders. TODO: Normal resolvers also.",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Draedr",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "redbird": "^0.10.0"
  }
}
